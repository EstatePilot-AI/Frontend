const SidebarGroup = ({ label }) => (
  <div className="px-4 pt-5 pb-2">
    <span className="text-[10px] font-bold tracking-[0.12em] uppercase text-(--color-text-muted)">
      {label}
    </span>
  </div>
)

export default SidebarGroup
